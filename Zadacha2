# - генерирует случайным образом список из 30 элементов в диапазоне от 1 до 30 и выводит его на экран,
# - формирует на основе этого списка новый список, в котором все двузначные элементы будут заменены их квадратами, а однозначные числа возведены в куб, и выводит его на экран,
# - вставляет перед каждым четным элементом число -10 и выводит список на экран, 
# - удаляет из списка все элементы с заданным значением N и выводит его на экран, 
# - добавляет перед элементом списка с номером M три нуля, а в конец списка – один элемент, равный сумме всех элементов списка, и выводит список на экран,
# - сортирует список по убыванию и выводит его на экран.



import random

print("Список из 30 случайных элементов от 1 до 30:")
s = [random.randint(1, 30) for i in range(30)]
print(*s)

print("Список, в котором двузначные элементы возведены во вторую степень, а однозначные в третью:")
m = []
for i in range(30):
    if s[i] > 9:
        m.append(s[i]**2)
    else:
        m.append(s[i]**3)
print(*m)

print("Перед каждым чётным элементом вставляется -10:")
for i in range(29, -1, -1):
    if m[i] % 2 == 0:
        m.insert(i, -10)
print(*m)

print("Введите N, и все элементы с таким же значением будут удалены из списка:")
N = int(input())
while N in m:
    m.remove(N)
print(*m)

print("Введите M, и перед элементом с этим номером добавятся три нуля, а в конец списка добавится сумма его элементов:")
M = int(input())
for i in range(3):
    m.insert(M - 1, 0)
m.append(sum(m))
print(*m)

print("Сортировка списка по убыванию:")
m.sort()
m.reverse()
print(*m)
